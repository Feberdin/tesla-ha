{
  "config": {
    "step": {
      "pick_implementation": {
        "title": "Select Tesla Fleet application"
      },
      "domain_input": {
        "title": "Register Tesla Fleet domain",
        "description": "Enter the public domain that hosts the Tesla public key. This domain must be configured as an allowed origin in the Tesla Developer Portal: {dashboard}",
        "data": {
          "domain": "Domain"
        }
      },
      "domain_registration": {
        "title": "Host Tesla public key",
        "description": "Place the following public key exactly at `{public_key_url}` and continue afterwards.\n\n```text\n{pem}\n```"
      },
      "registration_complete": {
        "title": "Pair Virtual Key",
        "description": "Open this link and add the Virtual Key in the Tesla app so signed vehicle commands can work:\n\n{virtual_key_url}"
      },
      "reauth_confirm": {
        "title": "Reconnect Tesla Fleet",
        "description": "Tesla requires a new login for {name}."
      }
    },
    "error": {
      "invalid_domain": "Invalid domain. Enter only the host name without https:// and without a path.",
      "invalid_response": "Tesla did not return a valid partner registration response.",
      "oauth_error": "Tesla Fleet OAuth failed. Check client ID, client secret and allowed origin.",
      "precondition_failed": "Tesla rejected the domain. Check the allowed origin and public-key URL in the Tesla Developer Portal.",
      "public_key_mismatch": "The public key read by Tesla does not match the local key.",
      "public_key_not_found": "Tesla could not fetch a public key for this domain.",
      "unknown": "An unknown error occurred."
    },
    "abort": {
      "already_configured": "Tesla Fleet is already configured.",
      "oauth_error": "Tesla Fleet OAuth failed."
    }
  }
}
